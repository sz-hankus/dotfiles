#!/bin/bash

# ls aliases
alias la='ls -alh'
alias ll='ls -l'

# cp (-v -> show what has been copied, -i -> ask to overwrite)
alias cp='cp -vi'

# cls
alias cls='clear'

# Other programs 
alias rng='ranger'
